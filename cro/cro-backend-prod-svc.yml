apiVersion: v1
kind: Service
metadata:
  name: cro-backend-monolith-prod-svc
spec:
  selector:
    app: cro-backend-monolith-prod-deploy
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
  type: LoadBalancer
