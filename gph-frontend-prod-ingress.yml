apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: gph-frontend-ingress
spec:
  rules:
  - host: gph.rbxrobotica.com.br
    http:
      paths:
      - pathType: Prefix
        path: "/"
        backend:
          service:
            name: gph-frontend-svc
            port:
              number: 80

